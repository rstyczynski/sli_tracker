#!/usr/bin/env node
// index.js — main entry point for sli-event-js action (no-op).
// OCI profile restore is done by a separate oci-profile-setup workflow step (local actions cannot use pre hooks).

'use strict';

process.exit(0);
