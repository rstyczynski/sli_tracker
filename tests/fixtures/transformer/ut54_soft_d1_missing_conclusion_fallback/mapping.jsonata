{
  "outcome": $exists(workflow_run.conclusion) ? workflow_run.conclusion : "unknown",
  "updated_at": workflow_run.updated_at,
  "repo": repository.full_name,
  "missing_fields": [
    $exists(workflow_run.conclusion) ? undefined : "workflow_run.conclusion",
    $exists(workflow_run.updated_at) ? undefined : "workflow_run.updated_at",
    $exists(repository.full_name) ? undefined : "repository.full_name"
  ]
}
